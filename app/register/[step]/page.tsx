import React from "react";

function RegisterStepsPage(props: { params: { step: string } }) {
  return <div>Steps Page {props.params.step}</div>;
}

export default RegisterStepsPage;

//shadcn [UI library] to install
//react hook form to install
